# Multi MCP Go Server

MCP Server in Go.

## Try the MCP
Pull the Docker image:
```
docker pull putuwaw/multi-mcp-go-server
```

Try the MCP using Postman:
```
docker run -i --rm --name mcp-server putuwaw/multi-mcp-go-server
```