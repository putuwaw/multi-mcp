# Multi MCP Python Server

MCP Server in Python.

## Try the MCP
Pull the Docker image:
```
docker pull putuwaw/multi-mcp-python-server
```

Try the MCP using Postman:
```
docker run -i --rm --name mcp-server putuwaw/multi-mcp-python-server
```